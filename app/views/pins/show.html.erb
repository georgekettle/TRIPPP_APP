<% if current_user %>
  <div id="pin" data-pin="<%= @pin %>"  data-currentuser="<%= current_user.to_json %>"></div>
<% else %>
  <div id="pin" data-pin="<%= @pin %>" ></div>
<% end %>

<%= javascript_pack_tag "pin" %>
